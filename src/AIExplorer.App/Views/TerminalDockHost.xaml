<?xml version="1.0" encoding="utf-8" ?>
<UserControl
    x:Class="AIExplorer_App.Views.TerminalDockHost"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="#1E1E1E">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Grid
            Padding="4,2,6,2"
            ColumnSpacing="4"
            Background="{ThemeResource SolidBackgroundFillColorTertiaryBrush}">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <ScrollViewer
                HorizontalScrollBarVisibility="Auto"
                VerticalScrollBarVisibility="Disabled"
                HorizontalScrollMode="Auto"
                VerticalScrollMode="Disabled">
                <StackPanel
                    x:Name="TabStrip"
                    Orientation="Horizontal"
                    Spacing="2"
                    VerticalAlignment="Center" />
            </ScrollViewer>

            <StackPanel
                Grid.Column="1"
                Orientation="Horizontal"
                Spacing="2"
                VerticalAlignment="Center">
                <Button
                    Style="{StaticResource AppToolButtonStyle}"
                    Click="OnAddClick"
                    ToolTipService.ToolTip="新建终端（当前目录）">
                    <FontIcon Glyph="&#xE710;" FontSize="12" />
                </Button>
                <Button
                    Style="{StaticResource AppToolButtonStyle}"
                    Click="OnRestartClick"
                    ToolTipService.ToolTip="重启当前终端">
                    <FontIcon Glyph="&#xE72C;" FontSize="12" />
                </Button>
                <Button
                    Style="{StaticResource AppToolButtonStyle}"
                    Click="OnClearClick"
                    ToolTipService.ToolTip="清屏">
                    <FontIcon Glyph="&#xE74D;" FontSize="12" />
                </Button>
                <Button
                    Style="{StaticResource AppToolButtonStyle}"
                    Click="OnCollapseClick"
                    ToolTipService.ToolTip="收起面板（保留会话）">
                    <FontIcon Glyph="&#xE70D;" FontSize="12" />
                </Button>
            </StackPanel>
        </Grid>

        <Grid x:Name="PaneHost" Grid.Row="1" />
    </Grid>
</UserControl>
